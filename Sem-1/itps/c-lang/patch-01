#include <stdio.h>

int sum(int p, int q)
{
    int r;
    r=p+q;
    printf("sum is = %d", r);
    return r;
    
}

int main()
{
    int a,b,c;
    printf("enter value of a: ");
    scanf("%d", &a);
    printf("enter value of b: ");
    scanf("%d", &b);
    c=sum(a,b);
    
}
